import { Game } from "../../src/types/GameTypes";

export const mockGame: Game = {
    id: '1',
    ruleset: {
        name: 'v1',
        version: 'v1'
    },
    timeout: 500
}

export const mockGame2: Game = {
    id: '2',
    ruleset: {
        name: 'v1',
        version: 'v1'
    },
    timeout: 500
}